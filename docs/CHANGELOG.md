# سجل التعديلات وتقرير آخر سحب

## آخر سحب من GitHub — 2026-08-17

- **من:** `9d43381`
- **إلى:** `cf00c30` (`origin/master`)
- **النشر على السيرفر:** تم في نفس اليوم
- **الحجم:** 5 commits — 40 ملفاً — +1815 / −225

### Commits

| Commit | العنوان |
|--------|---------|
| `196748e` | إصلاح رفع وسائط البرامج المنزلية ووصول ملفات المريض |
| `b24de04` | إظهار الأطباء على خريطة المريض + اختيار موقع في الأدمن |
| `ad50f03` | مستندات التحقق للأخصائي + شاشة الموافقة المعلّقة |
| `02e9a76` | إشعارات مسموعة في الخلفية + إمكانية حذفها |
| `cf00c30` | قبول كل أكواد الدول + تثبيت `intl` 0.20.2 |

---

### 1) البرامج المنزلية وملفات المريض (`196748e`)

**المشكلة:** الطبيب لا يرفق فيديو/رابط/صورة، والمريض لا يفتح المرفقات.

**الإصلاح:**
- حد الرفع كان 2MB لأن `.htaccess` مربوط بـ `mod_php` بينما السيرفر PHP-FPM → أُضيف `.user.ini` ودُعم `mod_php7/8`، والحد 500MB
- عمود `media_name` (migration `2026_08_17_000001`) حتى يظهر اسم الملف
- روابط الملفات من عنوان الطلب بدل `APP_URL` القديم
- فتح المرفق على أندرويد (كان `canLaunchUrl` يفشل بصمت)
- تعديل تمارين البرنامج دون مسح سجل الإنجاز
- معاينة الصور + رسالة واضحة عند خطأ 413

**ملفات رئيسية:** `HomeProgramController.php`, `ProgramExercise.php`, `public/.htaccess`, `public/.user.ini`, `create_program_page.dart`, `program_detail_page.dart`

---

### 2) خريطة المريض وموقع الطبيب (`b24de04`)

**المشكلة:** الخريطة فارغة لأن `latitude/longitude` عند الأطباء `NULL`.

**الإصلاح:**
- الموقع بالترتيب: إحداثيات الطبيب → العيادة → مركز المدينة (`CityCoordinates` — 60 مدينة)
- دبوس تقريبي إذا الموقع من المدينة فقط، مع إزاحة بسيطة حتى لا تتراكب الدبابيس
- في الأدمن: خريطة للنقر/السحب + زر مسح يعيد الاعتماد على المدينة

**ملفات رئيسية:** `TherapistSearchController.php`, `CityCoordinates.php`, `admin/therapists/edit.blade.php`, `TherapistController.php`

---

### 3) مستندات التحقق وشاشة الانتظار (`ad50f03`)

**المشكلة:** المستندات تُرفع مرة واحدة عند الإعداد وتُبتلع عند الفشل. بعد موافقة الأدمن تبقى الشاشة معلّقة.

**الإصلاح:**
- صفحة مستندات من الملف الشخصي (رفع / فتح / حذف)
- شاشة الانتظار تعيد فحص `/me` عند الفتح والاستئناف وكل 20 ثانية + زر تحقق
- بعد الموافقة ينتقل للرئيسية
- تسجيل الدخول يوجّه لشاشة الانتظار بدل حلقة `/auth` ↔ `/home`

**ملفات رئيسية:** `documents_page.dart`, `pending_approval_page.dart`, `login_page.dart`, `profile_page.dart`, `ProfileController.php`

---

### 4) الإشعارات (`02e9a76`)

**المشكلة:** الإشعارات (خصوصاً المكالمات) تصل بلا صوت في الخلفية، ولا يمكن حذف إشعار.

**الإصلاح:**
- قناة `call_channel` للمكالمات و قناة التطبيق لباقي الإشعارات (`channel_id` في FCM)
- `DELETE /notifications/{id}` و `DELETE /notifications/read`
- سحب للحذف + مسح المقروء في تطبيقي المريض والطبيب

**ملفات رئيسية:** `FcmService.php`, `routes/api.php`, `notifications_page.dart` (كلا التطبيقين), `AndroidManifest.xml`

---

### 5) أكواد الدول و`intl` (`cf00c30`)

**المشكلة:** التسجيل والدخول يقبلان فقط `+970` و `+972`. بناء Flutter يفشل إذا أُعيد `pub get` بسبب `intl`.

**الإصلاح:**
- قائمة 44 دولة مع بحث (`country_code_picker.dart`)
- التحقق من كود الدولة بالنمط بدل قائمة مغلقة
- `intl: ^0.20.2` في `pubspec.yaml` لكلا التطبيقين

**ملفات رئيسية:** `app_constants.dart`, `country_code_picker.dart`, `login_page.dart`, `phone_input_page.dart`, `AuthController.php`

---

## إصلاحات سابقة موثّقة (قبل هذا السحب)

### حذف الريل — شاشة سوداء (`9d43381`)

- السبب: `_load()` يضبط `_loading = true` على خلفية سوداء، وقد يُفتح مشغّل الفيديو بعد إغلاق الـ Dialog
- أُصلح في الريبو مع حذف البرامج وتعديل البرنامج

### Firebase 403 (`nabdh` vs `nabd`)

- `.env` كان `nabdh-3e72e` بينما credentials والتطبيقات `nabd-3e72e`
- صُحّح على السيرفر إلى `nabd-3e72e`
- خطأ 404 بعد ذلك = توكن FCM قديم للمستخدم؛ الحل خروج/دخول من التطبيق

### المحاكي لا يشغّل تطبيق الطبيب

- ليس خطأ في كود التطبيق
- المحاكي غير جاهز + `flutter run -d android` غير صالح
- الحل: انتظار `sys.boot_completed` ثم `flutter run -d emulator-5554`
- سكربت: `nabdh_therapist/scripts/run_emulator.sh`

---

## ما نُشر على السيرفر في هذا السحب

| الخطوة | النتيجة |
|--------|---------|
| `git fetch origin master` | `9d43381` → `cf00c30` |
| `rsync` (بدون `.env` / `storage` / `vendor`) | تم |
| `composer install --no-dev` | لا حزم جديدة |
| `php artisan migrate --force` | `add_media_name_to_program_exercises` DONE |
| كاش config/routes/views | تم |
| `config('services.firebase.project_id')` | `nabd-3e72e` |

---

## ما لم يُحدَّث بعد

- ملفات APK على سطح المكتب ما زالت من **2026-06-09** (`9d43381`)
- لتجربة تعديلات أغسطس على الجوال: أعد البناء وفق [MOBILE_BUILD.md](./MOBILE_BUILD.md)
